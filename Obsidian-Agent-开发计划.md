# Obsidian 个人知识 Agent 开发计划

## 1. 项目定位

构建一个运行于 Windows Obsidian 的个人知识 Agent。它以 Vault 内的 Markdown、PDF 和图片为知识来源，使用 DeepSeek 的文本能力、GLM 的视觉/OCR 能力和 Tavily 联网检索，完成以下闭环：

> 资料入库 → 带来源问答 → 受规则约束的笔记沉淀 → `daily/<主题>.md` 主题日志 → 按需知识回顾

项目面向个人使用并以 MIT 协议开源。插件免费，DeepSeek、GLM 和 Tavily 的用量由用户自备 API Key 和额度。

## 2. 已确认的产品决策

| 决策项 | 结论 |
| --- | --- |
| 目标平台 | Windows；第一版不承诺移动端 |
| 模型 | DeepSeek 处理文本；GLM 仅处理 PDF OCR 与图片视觉 |
| 联网能力 | Tavily 检索结构化网页摘要，再由 DeepSeek 输出直接回答 |
| 笔记来源 | Markdown、PDF、图片；可配置资料目录的后台增量索引 |
| 自动写入 | 允许，但必须受目录、操作类型和来源规则约束 |
| Daily 规则 | 按主题追加到 `daily/<主题>.md`，每条内容带时间戳 |
| Agent 记忆 | 长期记忆是用户可编辑的画像笔记；短期记忆是用户确认创建后持续追加的会话笔记。画像更新必须预览确认，会话笔记默认不参与全局检索。 |
| 权限 | 初始允许整个 Vault；用户可收窄读取、上传、联网、新建、追加、修改范围，并将敏感目录设为禁止外发 |

## 2.1 实施状态

### 已完成

- Obsidian TypeScript 插件骨架、侧边栏入口、插件设置与构建链路。
- 插件本地 `.env` 的 DeepSeek、GLM、Tavily API Key 加载与模型最小文本连通性请求；Key 不进入插件数据、审计日志或 Vault 笔记。
- 初始开放的权限策略：读取、向模型外发、附件索引、联网、新建 Inbox、追加 Daily、修改既有笔记均可独立收窄；敏感目录优先禁止 DeepSeek 和 GLM 外发。
- Vault 相对路径校验、路径穿越拒绝、写入目标目录约束与审计轨迹数据结构。
- 权限引擎单元测试与 TypeScript 打包验证。
- Markdown 标题感知切块、中文关键词检索、来源定位，以及仅向获授权片段发送的 DeepSeek 来源问答骨架。
- 统一的行动提案、Vault 写入预览与执行服务；Inbox 新建和主题 Daily 追加复用权限检查、路径处理、冲突检测和审计逻辑。
- 受控写入测试：预览不落盘、自动创建缺失目标目录、写入来源元数据、同名 Inbox 自动换用数字后缀、预览后内容变更即拒绝覆盖。
- DeepSeek 结构化笔记提案：模型仅输出 JSON 标题/主题和正文，插件附加已授权的 `SourceRef`、限制目标动作，并复用受控写入预览。
- PDF/图片附件索引：使用文件修改时间和大小作为增量指纹；PDF 调用 GLM-OCR、图片调用 GLM 视觉模型，解析文本作为可重建的派生缓存并接入来源检索；批处理队列可暂停/恢复，重启后恢复中断项，并提供请求限速及每日次数/上传体积预算。
- Tavily + DeepSeek 联网问答：Tavily 先检索网页摘要，再由 DeepSeek 直接回答；不在侧栏暴露网页 URL，内部来源用于写入溯源，并支持可选自动追加主题 Daily。
- Agent 对话优先界面：侧栏仅保留会话历史、对话内计划/写入确认卡片和固定底部输入框；索引、检索、联网、附件和编辑器能力继续保留，但只由 Agent 的白名单计划按需调用。
- 检索失败恢复：本地问答或 Wiki 问答零命中、来源不可外发、页面不可读，或模型判定缺少直接证据时，将结果作为无敏感正文的运行反馈自动触发一次替代计划；新计划禁止重复刚刚无效的本地/Wiki 查询，并在已确认的问答链中自动联网补证后给出完整回答。
- 联网无结果兜底：Tavily 成功但没有可用网页摘要时，默认仅对稳定知识降级为 DeepSeek 通用回答；实时倾向问题拒绝自动降级。降级回答不带网页来源，不能自动追加或作为联网结果写入。
- 同一外部操作的重复点击会复用进行中的请求，避免重复调用和扣费。
- 按需知识回顾：用户在对话中请求复盘或巩固主题时，复用带来源问答输出知识脉络、易混淆点、关联笔记和下一步；不维护关键词队列或间隔算法。
- 知识体系整合：基于当前笔记、搜索结果或指定文件夹先生成知识地图，再按需生成单个节点草稿；地图仅使用标题/短摘要，节点只发送关联片段。模型来源 ID 经本地校验，结果仅能新建于知识体系目录且必须预览确认。
- LLM Wiki：显式请求时复用知识地图的单次模型调用，将相关笔记编译成 `LLM Wiki/index.md`、`<主题>/概览.md`、`<主题>/概念/*.md` 和 `_system/Error Book.md`。主题和概念页面均回链到原始来源；注册表记录来源哈希、页面链接与主题新鲜度。问题命中已有 Wiki 时固定经过搜索、阅读、链接跟随和页面问答；未命中则回到原有的本地检索与联网兜底。原始资料变化会标记相关主题待更新。再次编译同主题只生成整组更新预览，不创建重复文件，也不改写原始笔记。
- 分层保真粘贴：普通 TSV、HTML 与 Markdown 表格本地规范化；合并单元格提供安全 HTML 备份；用户主动选择后才让 DeepSeek 修复已选中的异常格式，且结果必须预览确认。
- 知识库维护分析：只读巡检与目标相关的 Markdown，按真实来源输出重叠、冲突、缺口和过时项；不再维护嵌套任务。后续知识地图或写入由外层 Agent 使用现有工具安排，继续受预览与权限保护。
- 当前笔记关联补全：Agent 只对当前打开的 Markdown 工作；本地索引先排除已链接、敏感和未授权候选，再让模型在最多 12 篇候选中返回不超过 6 条有双边证据的语义关系。确认写入只替换插件托管的关联区块，保留用户正文和手写链接。
- 语义化 Agent Runtime：用户目标先被规划为至多四项受白名单约束的“工具 + 动作”步骤，覆盖索引、研究、整合、笔记、编辑器和诊断；Runtime 校验步骤依赖、至多允许一次重新规划，持久化状态和结果，并把插件中断的运行中步骤恢复为待执行。模型不直接调用工具，所有执行仍复用权限、预算、审计和写入预览边界。

### 待完成

- 在真实 Obsidian Vault 内验证 DeepSeek 文本、GLM 图片和 PDF/OCR、Tavily 检索请求。
- 完成 Markdown 检索结果的真实 DeepSeek 问答验收，包括模型来源标注质量检查。
- 为知识体系笔记增加来源片段哈希失效标记和按节点刷新能力。

## 3. 不可违反的设计原则

1. **Vault 是事实来源。** 重要笔记均为可读 Markdown；派生索引可以删除并重建。
2. **用户主导的本地权限。** 初始状态便于个人 Vault 全量使用；用户可以随时收窄范围，并用敏感目录永久阻断向外部模型外发。
3. **模型不直接写文件。** 模型只生成结构化行动提案；插件校验权限、解析路径、生成 diff 并执行。
4. **所有结论可溯源。** 基于 Vault、PDF、图片或网络的回答、笔记和按需回顾都附带来源定位。
5. **最小外发。** 文本片段只按需发送给 DeepSeek；PDF/图片只按需发送给 GLM；联网时 Tavily 只接收用户问题。
6. **派生内容可失效。** 原笔记或附件变化后，关联摘要和索引必须能够识别并重新生成。

## 4. 核心数据模型

### 4.1 SourceRef

所有内容统一映射为来源对象，供问答、自动写入和按需知识回顾使用：

```text
type + vaultPath/url + locator + contentHash + parserVersion + retrievedAt
```

示例：

- `note | Notes/机器学习.md | heading=损失函数 | hash=...`
- `pdf | 资料/paper.pdf | page=12 | hash=...`
- `image | 资料/diagram.png | hash=...`
- `web | https://example.com/... | retrievedAt=...`

### 4.2 ActionProposal

文本模型只能返回符合 JSON Schema 的行动提案，例如：

```json
{
  "type": "append_daily",
  "topic": "机器学习",
  "content": "记录内容",
  "sources": ["note:...", "web:..."]
}
```

插件负责将主题规范化为文件名、匹配权限、查重、生成 diff、写入、记录审计日志和提供撤销。

### 4.3 数据存储边界

- `00 Inbox/Agent/`：自动新建、待整理的笔记。
- `00 Inbox/Agent/Web Clips/`：附 URL、标题、抓取时间和摘要的网页沉淀。
- `daily/<主题>.md`：按主题持续追加的日志。
- 插件设置：模型、权限规则和少量队列状态；API Key 单独保存在插件本地 `.env`。
- 派生缓存：检索索引、缩略图、OCR/视觉结果；必须可重建且不作为唯一事实来源。

## 5. 分阶段计划与验收标准

### 阶段 0：技术验证

目标：在完整实现前消除 Obsidian、DeepSeek/GLM/Tavily 和附件管线的高风险不确定性。

- 初始化 TypeScript Obsidian 插件，验证设置页、侧边栏、Vault 读写与文件事件。
- 验证 DeepSeek 文本对话、GLM 图片理解、超时和错误反馈。
- 验证一张本地图片的 Base64 视觉请求。
- 验证一页普通 PDF 与一页扫描 PDF 的本地提取/OCR 方案。
- 验证派生缓存位置、清理和重建策略。

验收：能读取一条 Markdown、询问一张图片、解析一页 PDF；取消请求后无半写入或悬挂队列。

### 阶段 1：安全底座

目标：先建立 Agent 的边界，再开放实际能力。

- 设置页面：DeepSeek/GLM/Tavily API Key、模型、资料目录、预算、联网和权限规则。
- API Key 优先使用 Windows 安全存储；无法安全存储时允许每次启动输入。
- 实现 `PolicyEngine`：目录白名单、上传许可、联网、新建、追加、修改分别授权。
- 实现 `ActionLog`：记录行动类型、目标、来源、规则命中、时间与结果；日志不保存原始私密正文。
- 会话正文保存在用户确认创建的 Vault 会话笔记中；`data.json` 只保存活动会话和元数据，不保存完整对话。敏感目录中的会话/画像不会发送给模型。

验收：默认状态下所有写入和外发均被拒绝；越权目录、越权上传和越权写入均有可解释的拒绝记录。

### 阶段 2：Markdown 知识问答

目标：实现最小的可用知识问答闭环。

- 为 Markdown 建立标题、标签、链接、分段文本的增量索引。
- 使用关键词/结构化检索获取少量相关片段；第一版不上向量数据库。
- 侧边栏对话：回答基于 Vault 时必须渲染笔记链接和段落来源。
- 用户可切换“仅 Vault”“Vault + 模型常识”两种问答模式。
- 提供手动重新索引、索引状态和错误提示。

验收：针对 Vault 的回答可定位到原笔记；未找到依据时明确说明，不能伪造引用。

### 阶段 3：受控笔记沉淀

目标：使 Agent 可安全地形成新笔记和主题日志。

- `create_inbox_note`：仅在授权目录新建 Markdown，自动携带来源元数据。
- `append_daily`：将内容追加到 `daily/<主题>.md` 的时间戳小节。
- 主题归一与相似主题检测；低置信度时退回草稿，不创建新主题文件。
- 生成 Markdown diff；修改已有笔记必须由用户确认。
- 支持撤销最近的自动新建和自动追加。

验收：所有自动写入均有来源、规则命中和日志；同主题追加不产生重复文件；用户编辑期间不会被 Agent 覆盖。

### 阶段 4：附件与联网检索

目标：按权限处理资料目录中的 PDF、图片和外部来源。

当前实现：已完成 PDF/图片的增量索引，以及 Tavily 检索、DeepSeek 总结驱动的直接联网回答。网页 URL 不在侧栏展示，结构化来源仅保留于内部溯源；用户可将回答生成写入预览。用户单独开启“联网搜索后自动追加主题 Daily”后，才会在联网和 Daily 权限同时允许时自动追加。

- 资料目录扫描使用可暂停、可恢复、限速的持久队列；Obsidian 关闭后不继续运行。
- PDF：优先本地提取文本；扫描件、表格、公式或指定页才进入 OCR 队列。
- 图片：默认只记录路径、哈希和元数据；仅在规则允许时按需送入视觉模型。
- 附件以内容哈希和解析版本实现增量更新；文件变更会使旧派生结果失效。
- 联网搜索返回结构化标题、URL、发布日期和摘要；自动保存仅写入 Web Clips，且必须附完整来源。
- 增加单次任务、每日和队列层面的 Token/费用预算、暂停、恢复功能。

验收：未变化附件不会重复 OCR；额度耗尽时安全暂停；网页沉淀不缺 URL 和抓取时间。

### 阶段 5：按需知识回顾

目标：把回顾能力融入 Agent 对话，而不是维护独立队列。

- 用户直接请求“复盘/回顾/巩固某主题”；Agent 使用现有来源问答能力。
- 回顾输出固定为知识脉络、易混淆点或知识缺口、关联笔记和下一步；关联笔记使用 Vault 链接。
- 不维护复习日期、关键词、题卡、间隔算法或额外的持久化状态。

验收：回顾只依赖当前可检索来源；不产生新的复习队列或定时任务。

### 阶段 6：发布质量

目标：形成适合开源展示和求职演示的可靠产品。

- 完善 README：隐私边界、权限说明、DeepSeek/GLM/Tavily 配置、预算说明、故障处理和演示 GIF。
- 提供示例 vault 和默认规则，避免使用真实私人数据。
- 编写迁移、缓存清理和故障恢复说明。
- 添加版本号、变更日志、MIT License 和社区插件发布所需元数据。

验收：在干净 Vault 中安装后可完成核心 Demo；敏感内容和 API Key 不进入日志、测试素材或仓库。

## 6. MVP 之外的范围

以下能力不进入第一版，避免延迟核心闭环：

- 向量数据库、Embedding 和语义检索。
- FSRS 或其他复杂学习调度算法。
- 音频、视频和网页全文抓取。
- 多 Agent 深度研究与自动模型降级。
- 自动修改任意既有笔记。
- 模板市场、移动端和多设备同步优化。

## 7. 必测场景

1. **权限拒绝**：模型要求读取、上传或写入未授权目录时必须被拦截。
2. **提示注入**：笔记中的恶意指令不能覆盖系统权限和行动规则。
3. **并发编辑**：用户修改笔记期间，Agent 不能覆盖新版本。
4. **增量索引**：未变化 PDF/图片不得重复调用 GLM；修改后仅重建受影响内容。
5. **来源完整性**：自动写入或按需回顾缺少来源时拒绝输出为事实结论。
6. **撤销恢复**：自动新建 Inbox 与追加 Daily 均可恢复。
7. **预算恢复**：OCR/联网队列中断、取消或达到预算后可恢复，且不重复处理。
8. **可见 Agent 记忆**：长期画像和短期会话都以用户可编辑 Markdown 保存；模型只读取受权限允许且长度受限的当前会话与画像，不把会话回答作为全局知识事实。

## 8. 求职展示重点

项目不应定位为“又一个 RAG 聊天机器人”，而应重点展示：

- Agent 在真实用户文件系统中的最小权限执行与可审计写入。
- Markdown、PDF、图片和网页来源的统一溯源模型。
- 从资料入库到可追溯回顾的个人知识闭环。
- 成本、取消、队列恢复、缓存失效和测试覆盖等工程化能力。

推荐 Demo 链路：导入资料 → 索引 PDF/图片 → 带来源提问 → 自动追加 `daily/<主题>.md` → 展示原笔记与关键词 → 来源变化后刷新关键词。
